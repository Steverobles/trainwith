NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only — never exposed to the browser. Used for the guardian
# approval flow (bypasses RLS to read/write guardian_contacts + profiles).
SUPABASE_SERVICE_ROLE_KEY=
RESEND_API_KEY=
